<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head.ejs') %> 
</head>
<body class="bg-gradient-to-tr from-indigo-500 to-violet-500 debug-screens">
    

    <div class="block container mx-auto min-h-screen w-full bg-gray-100 shadow-2xl">
        <h1 class="flex justify-center text-5xl p-14 pb-4 font-bold text-indigo-600">
            <%= blog.title %> 
        </h1>
        <div class="flex justify-center">
            <div class="px-20 flex items-center space-x-4 p-8">
                <div class="rounded-full w-20 h-20 bg-cover bg-center" style="background-image: url('<%= blog['author.imageURL'] %>')"></div>
                <h2 class="text-3xl text-rose-600 font-medium"><%= blog['author.firstName'] %> <%= blog['author.lastName'] %></h2>
            </div>
        </div>
        <div class="px-16 whitespace-pre-line">
            <%= blog.body %> 
        </div>
        
    </div>
</body>
</html>